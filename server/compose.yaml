name: ckr-control

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "127.0.0.1:8088:8000"
    volumes:
      - ckr-data:/app/data
    restart: unless-stopped

volumes:
  ckr-data:
